{
  "name": "calendarbot-web",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "react-router-dom": "^6.24.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "deploy": "gcloud run deploy calendarbot-web --source=. --platform=managed --region=us-central1 --allow-unauthenticated --project=proyecto-laboratorio-467421",
    "logs": "gcloud run services logs read calendarbot-web --region=us-central1 --project=proyecto-laboratorio-467421 --limit=50"
  },
  "browserslist": {
    "production": [">0.2%", "not dead", "not op_mini all"],
    "development": ["last 1 chrome version", "last 1 firefox version", "last 1 safari version"]
  }
}
